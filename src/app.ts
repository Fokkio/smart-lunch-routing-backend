import cors from 'cors'; import express from 'express'; import { config } from './config/env'; import { errorHandler } from './middleware/error-handler'; import { customerRoutes } from './routes/customer.routes'; import { deliveryRoutes } from './routes/delivery.routes'; import { orderRoutes } from './routes/order.routes'; import { riderRoutes } from './routes/rider.routes'; import { routePlanRoutes } from './routes/route-plan.routes'; import { settingsRoutes } from './routes/settings.routes';
export function createApp():express.Express{const app=express();app.use(cors(config.corsOrigin?{origin:config.corsOrigin}:undefined));app.use(express.json());app.get('/api/health',(_req,res)=>res.json({status:'ok'}));app.use('/api/customers',customerRoutes);app.use('/api/orders',orderRoutes);app.use('/api/riders',riderRoutes);app.use('/api/deliveries',deliveryRoutes);app.use('/api/route-plans',routePlanRoutes);app.use('/api/settings',settingsRoutes);app.use(errorHandler);return app;}